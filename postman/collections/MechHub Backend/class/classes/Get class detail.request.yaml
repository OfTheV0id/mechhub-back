$kind: http-request
name: Get class detail
description: 获取班级完整信息。仅班级成员可调。返回 `membership_role` / `is_owner` 反映当前用户视角。
url: "{{baseUrl}}/api/classes/{{classId}}"
method: GET
headers:
  Accept: application/json
order: 1300
