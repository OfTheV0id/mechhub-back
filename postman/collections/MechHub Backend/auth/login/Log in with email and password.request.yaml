$kind: http-request
name: Log in with email and password
description: Verifies the password, requires `verified=true`, issues a session, and returns `userdata`. The response sets `session_id` as an HttpOnly cookie which Postman's cookie jar persists for subsequent requests on the same host.
url: "{{baseUrl}}/api/auth/login"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "email": "ahowevesybleberg@gmail.com",
      "password": "2823858458189c"
    }
order: 3000
