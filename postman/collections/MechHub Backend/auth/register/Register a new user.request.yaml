$kind: http-request
name: Register a new user
description: Creates a user with role `student` or `teacher`. Students receive an email verification token. Teachers require one admin to approve the `teacher_approval` token.
url: "{{baseUrl}}/api/auth/register"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "email": "ahowevesybleberg@gmail.com",
      "password": "2823858458189",
      "name": "ofthevoid",
      "role": "teacher"
    }
order: 1000
